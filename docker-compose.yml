services:
  api:
    build: .
    environment:
      APP_ENV: docker
      ARTIFACT_ROOT: /app/artifacts
      LOG_LEVEL: INFO
    ports:
      - "3002:3002"
    volumes:
      - ./artifacts:/app/artifacts
      - ./logs:/app/logs
